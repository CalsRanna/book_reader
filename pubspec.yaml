name: book_reader
description: A widget used to read a book.
version: 0.1.6
homepage: https://github.com/CalsRanna/book_reader
repository: https://github.com/CalsRanna/book_reader

environment:
  sdk: ">=2.18.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_android_volume_keydown: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
